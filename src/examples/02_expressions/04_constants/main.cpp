#include <iostream>
#include "constants.h"

using std::cout;
using std::cin;
int main()
{
    cout<<RATE<<"\n";
    auto amount = 0;
    
    cout<<"Enter num: ";
    cin>>amount;

    double result = calc_interset(amount):
    cout<<"result: "<<result<<"\n";
    return 0;
}